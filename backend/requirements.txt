# ============================================
# NeuroSpace Backend - Minimal Requirements
# Only packages directly imported in the code
# Using pinned versions from your old environment to prevent pip backtracking!
# ============================================

# --- Web Framework ---
fastapi==0.116.1
uvicorn==0.35.0
python-multipart==0.0.20

# --- Configuration ---
python-dotenv==1.0.1

# --- Database: Neo4j ---
neo4j==5.28.3

# --- Object Storage: MinIO (via S3 API) ---
boto3==1.42.45

# --- PDF Processing ---
pypdf==6.7.0
langchain-text-splitters

# --- Video/Audio Processing ---
faster-whisper==1.2.1

# --- LLM & RAG (LlamaIndex + Groq) ---
llama-index-core==0.14.14
llama-index-llms-groq==0.4.1
llama-index-embeddings-huggingface==0.6.1
llama-index-graph-stores-neo4j==0.7.0

# --- Caching ---
cachetools==5.5.2

# --- Async Patching ---
nest-asyncio==1.6.0

# --- ML Runtime (CPU-only torch is installed separately in Dockerfile) ---
sentence-transformers==3.0.1
